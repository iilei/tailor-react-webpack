/**
 * Created by iilei on 22.01.17.
 */

import unifyLocaleCode from './unifyLocaleCode';
import { camelizeKeys, camelize } from './camelize';

const utils = {
  unifyLocaleCode,
  camelizeKeys,
  camelize,
};

export {
  utils as default,
  unifyLocaleCode,
  camelizeKeys,
  camelize,
};
